# 数据库配置
# 本地开发使用 SQLite (不设置 DATABASE_URL 时使用)
# 生产环境设置 DATABASE_URL 连接 Neon PostgreSQL

# PostgreSQL 连接字符串 (生产环境使用)
# DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# 服务器端口
PORT=3001
